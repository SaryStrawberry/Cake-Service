package com.cakeservice;

public class FileUtil {
    public static void printAllRecipes() {
        //This method reads from the recipe file, parses each line, formats them and prints to screen
    }

    public static void printAllOrders() {
        //same as above but with orders
    }

    public static String formatNewRecipe(Cake cake) {
        //This method takes a cake and formats each attribute into a new recipe String
        return "";
    }

    public static void appendRecipe(String recipe) {
        //This method prints the recipe from the previous method into the recipe file
    }

    public static void appendOrder(Order order) {
        //this method prints the new order to the order file
    }
}
